name: Cluster deploy

on:
  workflow_call:
    inputs:
      org:
        description: "Organisation"
        required: true
        type: string
      environment:
        description: "Environment"
        required: true
        type: string
      image-tags:
        description: "Docker image with tag"
        required: true
        type: string

jobs:
  deploy-to-cluster:
    name: Deploy to Cluster
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Bake manifests with Kustomize
        id: bake
        uses: azure/k8s-bake@v4
        with:
          renderEngine: "kustomize"
          kustomizationPath: "kustomize/overlays/${{ inputs.environment }}/${{ inputs.org }}"

      - name: Substitute image into baked YAML
        id: substitute
        uses: azure/k8s-artifact-substitute@v2.0.0
        with:
          manifests: ${{ steps.bake.outputs.manifestsBundle }}
          images: ${{ inputs.image-tags }}

      - name: Stage substituted manifest in workspace
        id: stage
        shell: bash
        env:
          SUBSTITUTED_MANIFESTS: ${{ steps.substitute.outputs.directory }}
        run: |
          destination="$GITHUB_WORKSPACE/.k8s-deploy"
          mkdir -p "$destination"
          cp -R -- "$SUBSTITUTED_MANIFESTS/." "$destination/"
          echo "manifests=$destination" >> "$GITHUB_OUTPUT"

      - uses: azure/login@v3.1.0
        with:
          creds: "${{ secrets[format('AKS_{0}_FINT_GITHUB', inputs.environment)] }}"

      - name: Set the target cluster
        uses: azure/aks-set-context@v4.0.3
        with:
          cluster-name: "${{ vars[format('{0}_CLUSTER_NAME', inputs.environment)] }}"
          resource-group: "${{ vars[format('{0}_CLUSTER_RESOURCE_GROUP', inputs.environment)] }}"
          admin: "true"
          use-kubelogin: "true"

      - name: Deploy
        uses: azure/k8s-deploy@v7.0.1
        with:
          action: deploy
          manifests: ${{ steps.stage.outputs.manifests }}
          pull-images: false
          namespace: fintlabs-no
